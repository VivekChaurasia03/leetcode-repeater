# Admin secret for the /add-user endpoint.
# In dev (npm run dev): defaults to "admin" if not set.
# On Netlify: set this in Site Settings → Environment Variables.
ADMIN_SECRET=change-me-before-deploying
