[build]
  publish = "dist"
  command = "npm run build"

# LeetCode proxy — must come before the SPA catch-all
[[redirects]]
  from = "/api/problem/*"
  to = "/.netlify/functions/problem/:splat"
  status = 200

# SPA catch-all
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
