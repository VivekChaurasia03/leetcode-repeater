[build]
  publish = "dist"
  command = "npm run build"

# SPA catch-all — Netlify Functions v2 (config.path = '/api/*') take precedence
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
